
<h1>会員登録</h1>
<p>新規会員登録を行う画面です。<br>
下記項目を入力し、登録ボタンを押して登録してください。</p>

<table>
  <%= form_for(@userInfo, url:{controller: 'users_info', action: 'create'}) do |f| %>
    <tr>
      <th>氏名</th>
      <td><%= f.text_field :user_name %></td>
    </tr>
    <tr>
      <th>メールアドレス</th>
      <td><%= f.email_field :address %></td>
    </tr>
    <tr>
      <th>年代</th>
      <td><%= f.select :age, UsersInfo::AGES %></td>
    </tr>
    <tr>
      <th>職種</th>
      <td><%= f.select :job_m_id, @jobs %></td>
    </tr>
    <tr>
      <th>階級</th>
      <td><%= f.select :position_m_id, @positions %></td>
    </tr>
    <tr>
      <th>パスワード</th>
      <td><%= f.password_field :password %></td>
    </tr>
    <tr>
      <th></th>
      <td><%= f.submit "登録する", class: "btn btn-primary" %></td>
    </tr>
  <% end %>
</table>